/** DeepL `context` parameter — anchors ambiguous menu terms to food service meanings. */
export const DEEPL_CULINARY_CONTEXT =
  "This text is part of a restaurant menu. Translate all terms strictly within a culinary/food service context (e.g., 'tuna' must always translate to the fish 'atún' or equivalent noun based on the destination locale, not musical groups, general verbs, or idioms). Do not translate proper restaurant brand names or cross-referenced dish identifiers if flagged.";
